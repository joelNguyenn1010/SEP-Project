function borrowingProcess() {
    
  //This script handles determining if the user input is a UserID card or a scanned item
  //The script will then determine that, if its a UserID if its the start or end of a borrow, otherwise it will add the scanned item to a list of items to borrow and display the scanned item onto the screen
  //The script needs to somehow save the initial UserId and the list of scanned items

}